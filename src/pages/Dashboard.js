
const boxStyle = {
    color: "white",
    backgroundColor: "white",
    padding: "500px",
}

const Dashboard = () => {
    return (
        <div style={ boxStyle }>
            <h1 className = ".header">
                Dashboard Page
            </h1>
        </div>
    );
};

export default Dashboard;